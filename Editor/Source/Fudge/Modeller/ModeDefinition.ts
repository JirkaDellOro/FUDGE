namespace Fudge {
  export enum ControlMode {
    OBJECT_MODE = "ObjectMode",
    EDIT_MODE = "EditMode"
  }

  export enum InteractionMode {
    SELECT = "Select",
    TRANSLATE = "Translate",
    ROTATE = "Rotate",
    SCALE = "Scale",
    EXTRUDE = "Extrude",
    IDLE = "Idle"
  }
}
