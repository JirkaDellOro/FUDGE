namespace Fudge {
    import ƒ = FudgeCore;
    import ƒUi = FudgeUserInterface;
  
    export class ControllerAnimation extends ƒUi.Controller {
      public constructor(_mutable: ƒ.Mutable, _domElement: HTMLElement) {
        super(_mutable, _domElement);

      }
  
    }
  }